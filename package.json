{
  "name": "mahon-pool-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "sharp": "^0.33.0",
    "vitest": "^2.0.0"
  }
}
